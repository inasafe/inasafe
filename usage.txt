Usage:  accent.py [OPTION] (--vector-hazard=HAZARD_FILE | --raster-hazard=HAZARD_FILE)
                    (--download --layers=LAYER_NAME [LAYER_NAME...] | (--vector-exposure=EXP_FILE | --raster-exposure=EXP_FILE))
                    --extent=XMIN,YMIN,XMAX,YMAX --impact-function=IF_ID --report-template=TEMPLATE --output-file=FILE
        accent.py --list-functions
        accent.py --version

Arguments:
    FILE                    Output path for the impact layer and associated products.  If omitted,
                            the impact layer will be written into the current working directory.
    --list-functions        List the available impact functions and exit. Can be used along
                            with --exposure and --hazard options to produce a filtered list of
                            Impact Functions that will work with the specified hazard
                            and exposure layers.
Options:
    --raster-hazard=HAZARD_FILE    Hazard layer file such as a shapefile containing flood, tsunami,
                            volcano, earthquake data. The layer should be accompanied with an appropriate
                            keywords file.
    --vector-hazard=HAZARD_FILE    Hazard layer file such as a tif containing flood, tsunami,
                            volcano, earthquake data. The layer should be accompanied with an appropriate
                            keywords file.
    --vector-exposure=EXP_FILE     Exposure layer file such as a shapefile containing population,
                            structures (e.g. buildings), roads, etc. data. The layer should be accompanied with
                            an appropriate keywords file.
    --raster-exposure=EXP_FILE     Exposure layer file such as a tif containing population,
                            structures (e.g. buildings), roads, etc. data. The layer should be accompanied with
                            an appropriate keywords file.
    --extent=XMIN,YMIN,XMAX,YMAX      A quartet of decimal degrees values representing the analysis extent that
                            should be used. The order of the values is significant, representing XMIN, YMIN,
                            XMAX, YMAX. If omitted the union of the hazard and exposure layer extents should be used.
    --aggregation=AG_FILE   path to an aggregation layer such as a shapefile or tif containing village,
                            boundaries, town subdivisions etc. data. The layer should be accompanied with
                            an appropriate keywords file.
    --report-template=TEMPLATE     QGIS .qpt template file to be used for generating the PDF report.
                            If omitted, no PDF will be generated.  PDF output file will follow
                            prefix rules (see below).
    --prefix=PATH           prefix for the impact layer and associated products.  If omitted, a randomly
                            generated letter sequence will be used that is guaranteed to be unique.
    --impact_function=IF_ID Use one of the IDs given by the --list option.
    --version               Print the current version of InaSAFE.


# INSTALL on Ubuntu with:
# chmod ug+x accent.py
# sudo ln -s `pwd`/accent.py  /usr/bin